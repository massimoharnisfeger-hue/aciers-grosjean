# 22 septembre 2026


## Nuit — système d'information produit (495 fiches, boucle autonome)

- **Captures du propriétaire** (00:06-00:07) : prises sur le site déployé, qui est encore à `54d93a2`
  (17/09) — aucune correction d'hier n'y est. Le défaut de fond y est pourtant réel : fiche produit
  en paragraphes « • » sans titre ni liste, colonne « Description » vide sur 36 % de la largeur.
- **Cause racine, mesurée** : `descriptions-site-actuel.json` = 4 776 blocs, tous `p`, 2 556 puces
  littérales. Le générateur `integrer.py` ne reconnaissait ni une puce par `<p>` ni un intertitre en
  `<p>` nu. Corrigé dans le générateur (`structurer()`, lexique de titres, garde « Libellé Valeur »),
  JSON régénéré hors ligne depuis le cache du relevé : 494 titres, 721 listes ; `site-actuel.json`
  et `documents.json` inchangés. Contrôles D1-D3 vus rouges puis verts.
- **Système** (ADR-0008) : `lib/description.ts` classe les sections par famille (applications, points
  forts, caractéristiques, conseils, services) d'après l'intertitre ou l'amorce de liste, scinde les
  « libellé : valeur » ; `components/catalogue/FicheModules.tsx` rend chiffres clés (matière + spécs
  triées par ce qui définit la pièce + poids), listes à coches, chips, grilles libellé/valeur.
  Gabarit réordonné : identité → chiffres clés → schéma 3D coté (déjà présent pour 455 fiches) →
  prix → calculateur → spécifications → documents → CTA → « Le produit en détail » en deux colonnes.
  Sur mobile l'identité précède l'image. Aucune valeur inventée : chimie et brides restent sans
  chiffres clés, 37 fiches restent en prose.
- **Vérifié au navigateur** (six familles × 320/768/1280, trois builds) : zéro débordement, zéro
  puce, un H1, cibles tactiles à 0 en mobile, JSON-LD présent, catégorie inox sans régression.
  « Dans la même catégorie » vide sur capture pleine page = animation pilotée par le défilement,
  opacité 1 après défilement : pas une régression.
- **Au passage** : compteur « 40 ans » cassé à 320 px (`text-5xl` hérité) → `text-4xl sm:…`, R4.
- **Contrôles** : D1-D5 (`test_produit.py`), P9 (rendu réel de deux fiches), R4 ; registre
  `120 controles - 0 echecs`. Leçons L-023, L-024.
- **Reste** : calculateur en `mailto:` (P2, même défaut que H4) ; cotes lettrées non exploitées ;
  hauteur des fiches à 320 px (~7 500 px, dont ~2 100 de pied de page) ; fusionner la PR pour que le
  site déployé reflète enfin les corrections.
